# NTSlocal
NTSlocal is a lightweight local Wi-Fi file sharing server built with Node.js.  It allows users on the same network to upload any file via browser with drag &amp; drop,  real-time upload speed, connected users list, and activity logging — no internet required.
